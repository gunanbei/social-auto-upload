# 📝 本地开发指南

## 🎉 当前状态

✅ **后端**：运行在 Docker 容器中（端口 5409）  
✅ **前端**：运行在本地 Node.js 环境（端口 5173）  
✅ **前后端连接**：前端已配置连接到 `http://localhost:5409`  
✅ **无 Mock 数据**：前端直接调用真实后端 API

---

## 🚀 服务状态

### 查看运行中的服务

```bash
# 查看后端 Docker 容器
docker ps | grep social-auto-upload

# 查看前端进程
lsof -i :5173

# 测试后端 API
curl http://localhost:5409/getValidAccounts
```

### 访问地址

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:5409

---

## 🔧 服务管理

### 停止服务

```bash
# 停止后端 Docker 容器
docker stop social-auto-upload-backend

# 停止前端（在运行前端的终端按 Ctrl+C）
# 或者找到进程并结束
kill $(lsof -t -i:5173)
```

### 启动服务

```bash
# 启动后端 Docker 容器
docker start social-auto-upload-backend

# 启动前端
cd sau_frontend
npm run dev
```

### 重启服务

```bash
# 重启后端
docker restart social-auto-upload-backend

# 重启前端（先停止再启动）
kill $(lsof -t -i:5173)
cd sau_frontend && npm run dev
```

---

## 💻 前端开发

### 项目结构

```
sau_frontend/
├── src/
│   ├── api/              # API 接口定义
│   │   ├── user.js       # 用户相关 API
│   │   ├── account.js    # 账号管理 API
│   │   └── material.js   # 素材管理 API
│   ├── components/       # 组件
│   ├── views/           # 页面
│   ├── router/          # 路由
│   ├── stores/          # Pinia 状态管理
│   ├── utils/
│   │   └── request.js   # ⭐ HTTP 请求配置
│   └── styles/          # 样式
├── vite.config.js       # Vite 配置
└── package.json         # 依赖配置
```

### API 配置说明

前端 API 请求配置在 `src/utils/request.js`：

```javascript
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:5409',
  headers: {
    'Content-Type': 'application/json'
  }
})
```

**默认后端地址**：`http://localhost:5409` （正好是 Docker 容器的端口）

### 修改后端地址（可选）

如果需要连接到其他后端地址，可以：

**方法 1：创建环境变量文件**

创建 `sau_frontend/.env.local`（会被 git 忽略）：

```bash
# 开发环境后端地址
VITE_API_BASE_URL=http://localhost:5409
```

**方法 2：直接修改代码**

编辑 `sau_frontend/src/utils/request.js`：

```javascript
const request = axios.create({
  baseURL: 'http://your-backend-url:5409',
  // ...
})
```

### 热重载

前端支持热重载，修改代码后会自动刷新浏览器：

```bash
cd sau_frontend
npm run dev
```

### 构建生产版本

```bash
cd sau_frontend
npm run build
```

构建后的文件在 `sau_frontend/dist` 目录。

---

## 🐳 后端开发（Docker）

### 查看日志

```bash
# 实时查看日志
docker logs -f social-auto-upload-backend

# 查看最近 50 行日志
docker logs --tail 50 social-auto-upload-backend
```

### 进入容器调试

```bash
# 进入容器 shell
docker exec -it social-auto-upload-backend /bin/bash

# 在容器内执行 Python 命令
docker exec social-auto-upload-backend python3.10 -c "print('Hello')"
```

### 修改后端代码

如果需要修改后端代码，有两个选择：

**方法 1：修改本地代码后重启容器**

```bash
# 修改 sau_backend.py 或其他 Python 文件
# 然后重启容器
docker restart social-auto-upload-backend
```

**方法 2：停止容器，使用本地 Python 运行**

如果要在本地调试后端（需要 Python 3.10+）：

```bash
# 停止容器
docker stop social-auto-upload-backend

# 创建虚拟环境（首次）
python3.10 -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 运行后端
python sau_backend.py
```

**注意**：本项目已修改为兼容 Python 3.9+（将 `match` 语句改为 `if-elif`）

---

## 🔍 API 接口说明

### 主要 API 端点

| 接口 | 方法 | 说明 |
|------|------|------|
| `/getValidAccounts` | GET | 获取有效账号列表 |
| `/updateUserinfo` | POST | 更新用户信息 |
| `/deleteAccount` | GET | 删除账号 |
| `/getFiles` | GET | 获取所有素材文件 |
| `/uploadSave` | POST | 上传素材文件 |
| `/deleteFile` | GET | 删除素材文件 |
| `/upload` | POST | 临时上传文件 |
| `/postVideo` | POST | 发布视频 |
| `/postVideoBatch` | POST | 批量发布视频 |
| `/login` | GET | SSE 登录接口 |

### 测试 API

```bash
# 获取账号列表
curl http://localhost:5409/getValidAccounts

# 获取文件列表
curl http://localhost:5409/getFiles

# 上传文件（测试）
curl -F "file=@test.mp4" http://localhost:5409/upload
```

---

## 🐛 常见问题

### 1. 前端无法连接后端

**检查后端是否运行**：
```bash
docker ps | grep social-auto-upload
curl http://localhost:5409/getValidAccounts
```

**解决方案**：
```bash
docker start social-auto-upload-backend
```

### 2. 前端端口被占用

**错误信息**：`Port 5173 is already in use`

**解决方案**：
```bash
# 查找占用端口的进程
lsof -i :5173

# 结束进程
kill -9 <PID>

# 或者修改前端端口
# 编辑 sau_frontend/vite.config.js
server: {
  port: 5174,  # 改为其他端口
  open: true
}
```

### 3. 后端 API 返回 500 错误

**查看日志**：
```bash
docker logs social-auto-upload-backend --tail 50
```

**常见原因**：
- 数据库文件缺失
- 配置文件错误
- Cookie 文件路径问题

### 4. 跨域问题（CORS）

后端已配置 `flask-cors`，允许所有来源访问：

```python
from flask_cors import CORS
CORS(app)  # 允许所有来源跨域访问
```

如果遇到跨域问题，检查后端日志。

### 5. 前端热重载不工作

```bash
# 重启前端服务
cd sau_frontend
npm run dev
```

---

## 📚 开发工具推荐

### 前端

- **VSCode** + 插件：
  - Volar (Vue 3 支持)
  - ESLint
  - Prettier
- **浏览器工具**：
  - Vue DevTools (Chrome/Firefox 扩展)

### 后端

- **VSCode** + 插件：
  - Python
  - Pylance
- **API 测试**：
  - Postman
  - curl
  - httpie

---

## 🔄 开发流程建议

### 1. 前端开发流程

```bash
# 1. 拉取最新代码
git pull

# 2. 安装/更新依赖
cd sau_frontend
npm install

# 3. 启动开发服务器
npm run dev

# 4. 修改代码（自动热重载）
# 编辑 src/ 下的文件

# 5. 测试功能
# 在浏览器中打开 http://localhost:5173

# 6. 构建生产版本
npm run build
```

### 2. 后端开发流程

```bash
# 1. 查看当前容器状态
docker ps | grep social-auto-upload

# 2. 修改代码
# 编辑 sau_backend.py 或 myUtils/

# 3. 重启容器
docker restart social-auto-upload-backend

# 4. 查看日志验证
docker logs -f social-auto-upload-backend

# 5. 测试 API
curl http://localhost:5409/getValidAccounts
```

### 3. 调试技巧

**前端调试**：
```javascript
// 在代码中添加 console.log
console.log('API Response:', response)

// 使用 Vue DevTools 查看状态
// 使用浏览器 Network 面板查看请求
```

**后端调试**：
```python
# 在 sau_backend.py 中添加打印
print(f"Request data: {data}")

# 查看实时日志
docker logs -f social-auto-upload-backend
```

---

## 🎯 Git 工作流

```bash
# 1. 创建新分支
git checkout -b feature/your-feature

# 2. 修改代码并提交
git add .
git commit -m "feat: 添加新功能"

# 3. 推送到远程
git push origin feature/your-feature

# 4. 创建 Pull Request
# 在 GitHub 上创建 PR
```

---

## 📊 性能优化建议

### 前端优化

1. **使用路由懒加载**
2. **组件按需导入**（Element Plus 已配置）
3. **图片压缩和懒加载**
4. **使用 CDN**（生产环境）

### 后端优化

1. **使用数据库索引**
2. **添加缓存**（Redis）
3. **异步任务队列**（Celery）
4. **Nginx 反向代理**

---

## 🔐 安全注意事项

1. **不要提交敏感信息**
   - `.env` 文件已被 `.gitignore` 忽略
   - 不要提交 Cookie 文件
   - 不要提交数据库文件

2. **开发环境与生产环境分离**
   - 使用不同的配置文件
   - 生产环境使用环境变量

3. **定期更新依赖**
   ```bash
   npm update          # 前端
   pip install -U -r requirements.txt  # 后端
   ```

---

## 📖 相关文档

- [DEPLOYMENT.md](./DEPLOYMENT.md) - 详细部署指南
- [ENVIRONMENT.md](./ENVIRONMENT.md) - 环境要求说明
- [QUICKSTART.md](./QUICKSTART.md) - 快速开始指南
- [官方文档](https://sap-doc.nasdaddy.com/)

---

## 🆘 获取帮助

- **GitHub Issues**: https://github.com/dreammis/social-auto-upload/issues
- **交流群**: 关注公众号回复"上传"获取

---

**开发愉快！** 🎉

---

## 附录：快速命令参考

```bash
# ========== 服务管理 ==========
# 启动后端
docker start social-auto-upload-backend

# 停止后端
docker stop social-auto-upload-backend

# 重启后端
docker restart social-auto-upload-backend

# 启动前端
cd sau_frontend && npm run dev

# ========== 日志查看 ==========
# 后端日志
docker logs -f social-auto-upload-backend

# 前端日志（在运行 npm run dev 的终端查看）

# ========== 调试 ==========
# 进入后端容器
docker exec -it social-auto-upload-backend /bin/bash

# 测试后端 API
curl http://localhost:5409/getValidAccounts

# 查看端口占用
lsof -i :5409  # 后端
lsof -i :5173  # 前端

# ========== 开发 ==========
# 前端安装依赖
cd sau_frontend && npm install

# 前端构建
cd sau_frontend && npm run build

# 后端重启
docker restart social-auto-upload-backend

# ========== Git ==========
# 查看状态
git status

# 提交代码
git add .
git commit -m "feat: your message"
git push
```

